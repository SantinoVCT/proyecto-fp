{% extends 'base.html.twig' %}

{% block title %}Editar Carrito
{% endblock %}

{% block body %}
	{% include 'partials/navbar.html.twig' %}
	<div class="bg-dark py-4">
		<div class="container mx-auto px-4">
			<h1 class="text-6xl text-white text-center">
				Editar Carrito de
				{{ carrito.Usuario.nombre }}. Producto: {{ carrito.Producto.nombre }}
			</h1>
		</div>
	</div>
	<div class="container mx-auto px-4 py-8 m-3">
		<div class="bg-white rounded shadow relative p-3">
			{{ include('carrito/_form.html.twig', {'button_label': 'Cambiar'}) }}
		</div>
	</div>
{% endblock %}
