{% extends 'base.html.twig' %}

{% block title %}Tabla Producto
{% endblock %}

{% block body %}
	{% include 'partials/navbar.html.twig' %}
	<div class="bg-dark py-4">
		<div class="container mx-auto px-4">
			<h1 class="text-6xl text-white text-center">
				Producto
			</h1>
		</div>
	</div>
	{# Este bloque es para el mensaje de error #}
	{% for label, messages in app.flashes %}
		{% for message in messages %}
			<div class="bg-white rounded shadow overflow-hidden relative">
				<div class="px-6 py-4 mx-2">
					<p class="text-gray-700 text-base">
						{{ message }}
					</p>
				</div>
			</div>
		{% endfor %}
	{% endfor %}

	<div class="container mx-auto px-4 py-8">
		{% if productos is empty%}
			<div class="bg-white rounded shadow overflow-hidden relative">
				<div class="px-6 py-4">
					<div class="font-bold text-xl mx-2">No Products found</div>
					<p class="text-gray-700 text-base">
						There are no porducts in the database.
						<a href="{{ path('app_producto_new') }}" class="text-blue-500">Add one</a>
					</p>
				</div>
			</div>

		{% else %}
			<div class="px-6 py-4">
				<a href="{{ path('app_producto_new') }}" class="btn btn-primary px-4 py-2">Add One +</a>
			</div>
			<div class="d-flex justify-content-center bg-white rounded shadow relative p-2">
				<table class="table-responsive border-separate border-spacing-3 w-100 text-center">
					<thead>
						<tr class="border-dashed">
							<th>Id</th>
							<th>Nombre</th>
							<th>Descripcion</th>
							<th>Precio</th>
							<th>Acciones</th>
						</tr>
					</thead>
					<tbody>
						{% for producto in productos %}
							<tr>
								<td>{{ producto.id }}</td>
								<td>{{ producto.Nombre }}</td>
								<td>{{ producto.Descripcion }}</td>
								<td>{{ producto.Precio }}</td>
								<td>
									<div class="row g-3 justify-content-center">
										<a href="{{path('app_producto_show', {id: producto.id})}}" class="btn btn-info col-1 mx-1 w-auto">
											<i class="fas fa-eye"></i>
										</a>
										<a href="{{path('app_producto_edit', {id: producto.id})}}" class="btn btn-warning col-1 mx-1 w-auto">
											<i class="fas fa-edit"></i>
										</a>
										<form method="post" action="{{ path('app_producto_delete', {'id': producto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="col-1 mx-1">
											<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.id) }}">
											<button class="btn btn-danger w-auto">
												<i class="fas fa-trash"></i>
											</button>
										</form>
									</div>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		{% endif %}
	</div>
{% endblock %}
